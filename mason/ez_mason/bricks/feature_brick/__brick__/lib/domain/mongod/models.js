// TODO: export {{name.pascalCase()}}Model

/* Exporting the model to be used in other files.
module.exports = {
  {{name.pascalCase()}}Model: mongoose.model(COLLECTION.{{name.snakeCase()}}, require("./schemas/{{name.paramCase()}}-schema"))
};
*/
