// TODO: publish feature {{name.paramCase()}}

/*  Registering the routes from the `{{name.paramCase()}}-route.js` file.
module.exports = (app) => {
  app.use("/parent_path", require("../lib/interfaces/routes/{{name.paramCase()}}/{{name.paramCase()}}-route.js"));
};
*/
